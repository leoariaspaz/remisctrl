<%= form_for(@tipo_relleno) do |f| %>
  <% if @tipo_relleno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_relleno.errors.count, "error") %> prohibited this tipo_relleno from being saved:</h2>

      <ul>
      <% @tipo_relleno.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br>
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
